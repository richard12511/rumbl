defmodule Rumbl.Repo.Migrations.AddCatsTable do
  use Ecto.Migration

  def change do
    # create table(:cats) do
    #   add :name, :string, null: false
    #
    #   timestamps()
    # end
  end
end
